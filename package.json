{
  "name": "we-meet-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "make clean-dist && npm i && vite",
    "build": "make clean-dist && vite build",
    "preview": "make clean-dist && vite build --mode development && vite preview --mode development"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.3",
    "@vue/tsconfig": "^0.7.0",
    "vite": "^6.3.5",
    "vue-tsc": "^2.2.8",
    "typescript": "~5.8.3",
    "vue-router": "^4.5.1"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "webrtc-adapter": "^9.0.3"
  }
}
